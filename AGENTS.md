# server-tracker — инструкции для агента

Система учёта и трассировки серверных платформ собственного производства.
Объём: 50–300 юнитов/год. Разворачивается self-hosted (Docker Compose) на
инфраструктуре заказчика. Приоритет — надёжность и целостность данных,
не производительность под нагрузкой (нагрузка низкая).

## Общие качества агента

- Кратко, по делу — не растекаться мыслью по древу.
- Внутренние рассуждения — на английском, ответ пользователю — на
  русском (не путать с языковой конвенцией ниже — та про
  веб-интерфейс и код, а не про общение с пользователем).
- Перепроверять свои заявления перед тем, как выдавать их за факт, и
  критически относиться к входной информации (в т.ч. к словам самого
  пользователя) — не подстраивать ответ под ожидания молча.
- Не подхалимничать: указывать на проблемы и слабые места, даже если
  это значит не согласиться с пользователем.

## Языковая конвенция — НЕ НАРУШАТЬ

- **Веб-интерфейс** (всё, что рендерится в браузере: Jinja-шаблоны,
  надписи, сообщения об ошибках, которые видит пользователь) — **на
  русском языке**.
- **Всё остальное** (Python-идентификаторы, комментарии, докстринги,
  имена колонок БД, значения enum/status-кодов, CLI-скрипты,
  сообщения в логах) — **на английском языке**.
- Единая точка перевода backend-кодов в русские подписи —
  `app/i18n.py`. Любой новый код (`PlatformItem.status`,
  `PlatformEvent.event_type` и т.д.) добавляется туда же самим
  изменением, которое его вводит. Никогда не хардкодить переведённую
  строку прямо в шаблоне или роутере — искать/добавлять перевод только
  через `app/i18n.py`. Исключение — категории деталей
  (`part_categories`), это пользовательский каталог в БД, а не
  захардкоженный enum, см. «Категории деталей» ниже.
- Jinja-фильтр `label` уже зарегистрирован в `app/templating.py`
  (`{{ p.status | label('platform_status') }}`). Импортировать
  `templates` из `app.templating` во всех роутерах, не создавать
  локальные `Jinja2Templates(...)` инстансы.

## Стек (не менять без явного обсуждения)

- PostgreSQL 16 — основное хранилище
- Python 3.12 + FastAPI — backend
- SQLAlchemy 2.0 (Mapped/mapped_column style) + Alembic — ORM и миграции
- Jinja2 + HTMX + чуть-чуть Alpine.js — фронтенд. Никакого React/SPA:
  проект должен оставаться server-rendered, чтобы оставаться понятным
  человеку без опыта в вебе.
- Сессионная аутентификация (cookie), пароли — argon2
- Docker Compose: app + postgres + caddy (reverse proxy / TLS)

## Ключевой архитектурный принцип — НЕ НАРУШАТЬ

Компоненты (память, CPU, riser-карты и т.д.) никогда не привязываются к
изделию напрямую колонками вида `cpu1_serial`. Связь идёт только через
таблицу `platform_components` с полями `installed_at` / `removed_at`.
Это единственный способ:
1. поддержать переменное число компонентов одного типа на изделие
   (N планок памяти, N карт в райзере);
2. сохранить полную историю при замене компонента (RMA) —
   `removed_at IS NULL` означает "установлен сейчас", любая другая
   строка — историческая запись.

Обратный поиск "по серийнику компонента найти все изделия, где он
когда-либо стоял" — основной пользовательский сценарий. Индексы на
`part_units.serial_number` и `platform_items.asset_tag` обязательны.

### Иерархия «Платформа → Исполнение → Изделие» — НЕ НАРУШАТЬ

Три уровня, соответствующие трём разным вопросам:

- **Платформа** (`platforms`, класс `Platform`) — продуктовая линейка,
  которую разрабатывают, например «2U Storage». Только name/description,
  без деталей комплектации. Верхний уровень навигации в UI (`/platforms`).
- **Исполнение** (`platform_variants` + `platform_variant_slots`,
  классы `PlatformVariant`/`PlatformVariantSlot`) — конкретная
  BOM-конфигурация внутри платформы, например «24-bay all-flash» и
  «12-bay hybrid» под одной платформой. Это «конструктор»: эталон
  (as-planned) — какие элементы должно иметь изделие данного
  исполнения (CHASSIS, MOTHERBOARD, MIDPLANE, BACKPLANE_FRONT,
  BACKPLANE_REAR, IO_BOARD, USB_BOARD, PSU, CPU, DIMM, RISER_CARD, DISK
  и т.д.), с quantity и required. Задаётся один раз при заведении
  исполнения и дальше не меняется для него. У одной платформы может
  быть несколько исполнений. В коде/БД это по-прежнему «slot»
  (`PlatformVariantSlot`, `slot_name`, `platform_variant_slot_id` —
  английские идентификаторы по языковой конвенции), но в русском
  UI/тексте — **«элемент изделия»**, не «слот» (переименовано по
  просьбе пользователя).
- **Изделие** (`platform_items`, класс `PlatformItem`) — конкретный
  собранный физический юнит с asset tag, `platform_variant_id`
  указывает, какого он исполнения. `platform_components` — факт: что
  реально установлено сейчас/было установлено в этом изделии. Поле
  `platform_variant_slot_id` (опционально) связывает конкретную
  установку с эталонным элементом исполнения — это даёт бесплатную
  проверку комплектности (сравнить count(active) по каждому элементу с
  его quantity) без дублирования структуры вручную для каждого изделия.

Не создавать отдельные "фиксированные" колонки под platform_variants для
каждого типа платы (midplane_part_type_id и т.п.) — вся вариативность
шасси должна укладываться в строки platform_variant_slots.

UI-путь строго вложенный: `/platforms` (список платформ) →
`/platforms/{id}` (список исполнений этой платформы) → `/variants/{id}`
(конструктор элементов + список изделий этого исполнения) → `/items/{id}`
(карточка изделия: чек-лист, компоненты). Список изделий смотрят
только внутри конкретного исполнения, не через общий список всех
исполнений — так решил пользователь. Отдельно есть плоский `/`
(«Изделия») — глобальный дашборд по всем изделиям сразу с поиском по
серийнику/asset tag, для быстрого оперативного доступа в обход
иерархии.

### Категории деталей (`part_categories`) — пользовательский каталог, не enum

В отличие от всех остальных кодов в проекте (роли, статусы, типы
событий/прошивок — см. «Языковая конвенция»), категории деталей **не**
захардкожены в `app/i18n.py`. Они живут в таблице `part_categories`
(`name`, `group`) и редактируются прямо из интерфейса на
`/part-categories`, а также инлайн-формой «+ Добавить свою категорию»
на странице исполнения (`/variants/{id}`) — новую физическую форм-фактор
детали инженер заводит сам за 10 секунд, без участия агента/разработчика
и без деплоя. `name` — сразу русский текст (как `Platform.name`), без
отдельного английского кода.

Две фиксированные группы (`group`, значения `custom`/`purchased`,
подписи — `PART_CATEGORY_GROUPS` в `app/i18n.py`):
- **`purchased`** («Покупное») — готовые компоненты с рынка: PCIe/OCP
  карты, DDR, CPU, PSU, SSD M.2, диски LFF/SFF, райзер-карты.
- **`custom`** («В составе изделия») — платы собственной разработки,
  часть конструкции самого изделия: материнская плата, шасси, мидплейн,
  бэкплейн (передний/задний), IO-плата, USB-плата.

`part_types.category_id` и `platform_variant_slots.category_id` — обе
FK на `part_categories.id`, это единый список категорий и для каталога
деталей, и для элементов конструктора.

Элемент проверяется на установке только по категории — и то лишь
косвенно, через выбор конкретного слота в форме (на уровне сервиса
категория/тип детали при установке компонента не валидируется вовсе).
Раньше у `PlatformVariantSlot` было ещё поле `part_type_id` для жёсткой
привязки слота к одной модели платы — убрано: оно нигде не
проверялось при установке (можно было поставить любую деталь в любой
слот), то есть было чисто декоративным. Если понадобится реальная
привязка к модели — делать вместе с валидацией на установке, не как
неиспользуемое поле в конструкторе.

**Область видимости категории** — `part_categories.platform_variant_id`
(nullable): `NULL` — глобальная категория, видна в конструкторе любого
исполнения (сидированный стартовый набор выше); заполнено — категория
видна только в конструкторе этого конкретного исполнения. Категория,
добавленная инлайн-формой на странице исполнения, автоматически
получает `platform_variant_id` этого исполнения — так специфичная для
одного шасси категория не засоряет выпадающий список у всех остальных
исполнений. Уникальность `name` — двумя частичными индексами в БД
(глобально среди `NULL`, и отдельно в рамках каждого исполнения), а не
простым `unique`-столбцом: `NULL != NULL` в обычном уникальном индексе
позволил бы двум разным глобальным категориям иметь одинаковое имя.
Тот же паттерн (global vs variant-scoped) — у `firmware_types` ниже.
Удаление (`DELETE /part-categories/{id}/delete`) разрешено только если
категория нигде не используется (`part_types`/`platform_variant_slots`)
— иначе понятная ошибка, не потеря данных.

### Название и ревизия платы

`part_types.model_name` — внутреннее название модели детали,
`part_types.revision` (nullable) — её ревизия/версия. Это касается в
первую очередь плат собственной разработки (midplane, backplane_front,
backplane_rear, io_board, usb_board и подобных, в отличие от покупных
CPU/RAM/NIC, у которых модель — это готовый order code от
производителя): ревизия платы напрямую влияет на совместимость между
сборками и должна быть частью идентичности PartType, а не отдельной
пометкой в notes.

### Событийный лог изделия (вместо статичных дат)

Даты жизненного цикла конкретного собранного изделия (укомплектовано,
тестирование начато/завершено, отгрузка и т.д.) хранятся не как
колонки в `platform_items`, а как строки в `platform_events`
(`platform_item_id`, `event_type`, `occurred_at`, `user_id`, `notes`).
Причины:
1. новый тип события не должен требовать миграции схемы;
2. этап может повторяться (например, повторное тестирование после
   ремонта по RMA) — одна колонка-дата этого не выдержит, лог — да.

Реализовано на отдельной странице `/items/{id}/stages` (ссылка «этапы →»
рядом со статусом на карточке изделия — сами кнопки туда не вынесены,
чтобы не загромождать карточку) — кнопка на каждый этап, которая
проставляет `occurred_at = now()` и текущего пользователя, а не форма
для ручного ввода даты. Результат теста — не отдельная колонка, а три
разных `event_type` (`test_passed`/`test_passed_with_remarks`/
`test_failed`) вместо одного общего "завершено", чтобы не городить
миграцию под каждый новый вариант исхода; для `test_passed_with_remarks`
заметка (описание замечаний) обязательна на уровне сервиса. Лента
событий — на `/items/{id}/history` (там же снятые компоненты; на
`/items/{id}/stages` — только последние 5, для контекста). Каждый новый
`event_type` регистрируется с русской подписью в `app/i18n.py`
(`PLATFORM_EVENT_TYPES`). Текущий набор (порядок процесса): `assembled`
(Укомплектовано), `test_started` (Тестирование начато), `test_passed`
(Тест пройден), `test_passed_with_remarks` (Тест пройден с
замечаниями), `test_failed` (Тест не пройден), `shipped` (Отгружено) —
заменил изначальный плейсхолдерный набор
(`manufactured`/`qc_verified`/`initial_test`/`final_test`) на реальный
процесс, который описал пользователь.

Порядок процесса обеспечен явными проверками в
`app/services/platform_events.py` (`_PREREQUISITES`), а не просто
порядком кнопок: `test_started` требует, чтобы уже было `assembled`;
любой из `test_passed`/`test_passed_with_remarks`/`test_failed`
требует `test_started`; `shipped` требует `test_passed` ИЛИ
`test_passed_with_remarks` (нельзя отгрузить непройденный тест).
Несоблюдение — ошибка пользователю, а не тихий пропуск.

`platform_items.status` остаётся отдельным (денормализованным) полем
"текущий этап" для быстрой фильтрации списков — `app/services/platform_events.py`
держит его в синхроне при записи события: `test_started` → `status="testing"`,
`shipped` → `status="shipped"`; остальные события статус не меняют (это
подэтапы внутри "testing"/"assembly", лог — источник истины по точным
датам, `status` — грубый фильтр для списков).

### Прошивки — как элемент конструктора

Таблица `firmware_records` — тот же принцип, что и `platform_events`:
append-only лог версий прошивок для `part_unit` (материнская плата:
BIOS/BMC/CPLD; бэкплейн: своя прошивка; в будущем — любая другая плата),
а не колонки `bios_version`/`bmc_version` на part_units. Причины те же:
новый тип прошивки не требует миграции, а история версий важна не
меньше текущей (для расследования отказов нужно знать, какая версия
стояла в момент выхода из строя).

`firmware_type_id` — FK на `firmware_types` (тот же паттерн
пользовательского каталога global/variant-scoped, что и
`part_categories`, см. выше; сид: BIOS, BMC, CPLD, «Прошивка
бэкплейна» — глобальные). Не захардкожено в `app/i18n.py`, в отличие от
остальных enum-кодов проекта.

`image_slot` (`primary`/`backup`) решает случай "две прошивки BIOS/BMC,
которые могут быть разных версий" (dual-BIOS/dual-BMC редундантность) —
каждый образ версионируется независимо. Для прошивок с одним образом
(CPLD, прошивка бэкплейна) `image_slot` всё равно заполняется значением
`primary` (не NULL) — так текущая версия ищется одним и тем же запросом
для всех типов, без специального случая на NULL.

Текущая версия — последняя по `recorded_at` строка для пары
`(part_unit_id, firmware_type_id, image_slot)` (запрос через
`DISTINCT ON` в PostgreSQL, см. докстринг модели).

**Прошивка как требование исполнения.** Таблица
`platform_variant_firmware_requirements` (`platform_variant_id`,
`firmware_type_id`, `track_backup`) — конструктор-эквивалент
`platform_variant_slots`, но для прошивок вместо физических деталей:
задаётся на странице исполнения (`/variants/{id}`), сама наличие строки
означает «основной образ обязателен», `track_backup=true` — «резервный
образ тоже отслеживается, но не обязателен» (dual-BIOS/BMC: один
обязателен, второй опционален — так решил пользователь). На карточке
изделия (`/items/{id}/firmware-mac`, отдельная страница, чтобы не
загромождать карточку — та же логика, что у `/items/{id}/stages`) —
чек-лист по требованиям исполнения (какая версия записана на каком
установленном компоненте) + форма записи новой версии. Записать версию
можно только для типа прошивки, заданного как требование этого
исполнения, и только на деталь, реально установленную в это изделие
сейчас (`app/services/firmware_records.py`). Деталь дополнительно
должна быть категории группы `custom` («в составе изделия») — прошивка
живёт на платах собственной разработки (материнская плата, бэкплейн и
т.д.), а не на покупных деталях (CPU, RAM, PSU и т.д.); проверка на
уровне сервиса, не только скрытие опции в форме.

### MAC-адреса — как элемент конструктора

Таблица `mac_addresses` — MAC-адрес принадлежит либо изделию
(management/BMC-порт на уровне шасси), либо конкретному `part_unit`
(типично материнская плата: LAN1/LAN2/BMC, но в принципе любая
сетевая карта). Ровно один из `platform_item_id`/`part_unit_id`
заполнен — это гарантируется CHECK-constraint `num_nonnulls(...) = 1`
в БД, не полагаться на проверку только в Python. Не ограничивать число
MAC на владельца двумя колонками — по факту их бывает 2, но таблица
должна пережить появление 3-го интерфейса без миграции схемы.
Нормализацию формата (`AA:BB:CC:DD:EE:FF`, верхний регистр) делать в
service-слое перед записью (`app/services/mac_addresses.py`), не
полагаться на формат ввода пользователя.

`mac_addresses` — не append-only лог (в отличие от `platform_events`/
`firmware_records`): удаление MAC — реальный `DELETE`, не
`removed_at`-маркер, и строка не несёт своего `user_id`/времени
действия — поэтому мутации здесь идут через `audit_log`, как
`platform_items`/`platform_components`, а не мимо него.

**MAC как требование исполнения.** Таблица
`platform_variant_mac_requirements` (`platform_variant_id`, `label`,
`required`) — конструктор-эквивалент `platform_variant_slots` для
сетевых интерфейсов: задаётся на странице исполнения, например «BMC»
обязателен, «LAN» опционален (минимум один MAC — обязательно и
привязан к BMC, второй опционален — так решил пользователь). Фактическая
привязка MAC к требованию — по совпадению `mac_addresses.label` с
`label` требования (без учёта регистра), не отдельный FK. Чек-лист и
управление MAC — на той же странице `/items/{id}/firmware-mac`, что и
прошивки.

## Схема данных

См. `alembic/versions/` — это источник истины по схеме: 0001 создаёт
базовую схему, 0002 добавляет `api_tokens`, 0003 расширяет
`audit_log.entity_type` до 64 символов (самое длинное имя сущности —
`platform_variant_firmware_requirement`, 37 символов). Таблицы: users, api_tokens,
platforms, platform_variants, part_categories,
firmware_types, part_types, part_units, firmware_records,
platform_variant_slots, platform_variant_firmware_requirements,
platform_variant_mac_requirements, platform_items, platform_components,
platform_events, mac_addresses, audit_log, login_attempts, attachments
(19 таблиц;
platforms/platform_variants/platform_items — иерархия Платформа→
Исполнение→Изделие, см. выше; platform_variant_slots/
platform_variant_firmware_requirements/platform_variant_mac_requirements
— конструктор исполнения для физических деталей/прошивок/MAC
соответственно, см. соответствующие разделы выше).

## Роли и доступ

- `admin` — всё, включая управление пользователями (`/users`, см. ниже)
  и выпуск/отзыв токенов API (`/api-tokens`)
- `engineer` — CRUD по всей иерархии платформ (платформы/исполнения/
  изделия) и компонентов
- `viewer` — только чтение; в шаблонах для этой роли поле
  `platform_items.customer` и любые коммерческие заметки должны
  скрываться — сейчас это чисто server-rendered Jinja (не JSON API), так
  что фильтрация в шаблоне ({% if user.role != 'viewer' %}) и есть
  эквивалент "не сериализовать" — но при появлении JSON-эндпоинтов
  фильтровать нужно будет в schemas/, не полагаться на фронтенд.

Гостевой доступ = роль `viewer` с обычным логином (общий/shared
аккаунт, если нужно). Отдельного анонимного пути нет — это осознанное
решение, чтобы вся авторизация шла через один middleware.

### Управление пользователями (`app/routers/users.py`, `app/services/users.py`)

Реализовано: список, создание, смена роли, деактивация/активация,
сброс пароля. Всё — только `require_role("admin")`.

«Удаление» пользователя — это **деактивация** (`is_active=False`), не
`DELETE`-строка: на `users.id` ссылаются `firmware_records.user_id`,
`platform_events.user_id`, `audit_log.user_id` (nullable FK, без
`ON DELETE CASCADE`) — физическое удаление либо упадёт на
FK-constraint, либо (если когда-нибудь добавят `SET NULL`) молча
обезличит исторические записи о том, кто что сделал. Это прямое
применение принципа из раздела «Устойчивость к изменениям» ниже —
не частный случай, а тот же принцип.

Защита от блокировки системы: нельзя деактивировать/понизить в роли
самого себя (`SelfLockoutError`) и нельзя деактивировать/понизить
последнего активного `admin` (`LastAdminError`) — иначе некому будет
зайти и всё починить. `app/create_admin.py` остаётся нужен для
бутстрапа первого admin'а на чистой БД, где создавать пользователя
через UI ещё некому.

## Аудит

Любое создание/изменение/удаление part_units, platform_items,
platform_components, а также пользователей (создание, смена роли,
активация/деактивация, сброс пароля) должно писать запись в audit_log
(кто, что, когда, diff в JSONB). Реализовать как явный вызов
сервисного слоя (`app/services/audit.py`), не как магические
ORM-события — так проще ревьюить человеку, не являющемуся
веб-разработчиком. Platforms/platform_variants (сам каталог платформ и
исполнений) не аудируются — это справочные данные, не складской учёт.

## Устойчивость к изменениям — сохранность данных при миграциях

БД будет меняться на всём протяжении проекта (новые статусы, новые типы
событий/прошивок, новые элементы в конструкторе и т.д.) — это ожидаемый
рабочий процесс, а не исключение. При любом изменении схемы или кода,
затрагивающем существующие данные, приоритет — сохранить текущее
содержание максимально полно:

- Миграция обязана быть **сохраняющей данные** по умолчанию: `ALTER
  COLUMN` вместо `DROP` + `ADD`, `op.alter_column(..., new_column_name=)`
  вместо удаления и создания заново, backfill для новых NOT NULL
  колонок (сначала nullable + заполнение данными отдельным `UPDATE`,
  потом `NOT NULL` constraint отдельным шагом), явный маппинг старых
  enum-значений на новые при сужении списка допустимых значений (не
  просто отбрасывать те, что вышли из списка).
- Если сохранить данные при миграции в принципе невозможно (например,
  тип колонки несовместим и конвертация теряет точность) — это не
  решается молча удалением: нужно явно сообщить об этом пользователю в
  ответе и предложить план (например, перенести старое значение в
  отдельную колонку/JSONB-архив вместо того, чтобы просто его потерять).
- `downgrade()` в каждой Alembic-миграции должен быть реализован
  по-настоящему (не `pass`) и быть безопасным для существующих строк —
  даже если откат неизбежно теряет часть данных, добавленных уже после
  апгрейда, он не должен падать или портить то, что было до апгрейда.
- Прежде чем писать `DROP TABLE`/`DROP COLUMN` — проверить, не унесёт
  ли это единственную копию исторических данных. Append-only логи
  (`platform_components`, `platform_events`, `firmware_records`,
  `audit_log`) по определению самые ценные данные в системе — никогда
  не усекать и не перезаписывать их без явного запроса пользователя.
- То же касается рефакторинга кода приложения (не только схемы): не
  менять правила валидации/сериализации так, чтобы существующие записи
  в БД стали нечитаемыми или невалидными по новым правилам — либо
  писать миграцию данных под новый формат, либо явно поддерживать
  старый формат наравне с новым.
- Перед любой миграцией/изменением, способным затронуть существующие
  данные, — явно проговорить это пользователю (что теряется, что
  сохраняется, какой план) до применения, а не постфактум.

### Миграция 0001 заморожена

`alembic/versions/0001_initial_schema.py` больше не редактируется —
любое изменение схемы оформляется новой ревизией (`0002_...` и далее).
До этого 0001 переписывался задним числом 12 раз; работало это только
потому, что базу каждый раз создавали заново. На базе, где миграция уже
применена, такой подход не даёт применить ни одного последующего
изменения: `alembic_version` остаётся `0001`, `alembic upgrade head`
не находит, что применять, а код уже рассчитывает на новые колонки.

Страховка — `alembic check`: он сравнивает `Base.metadata` с живой
схемой. `scripts/deploy.sh` вызывает его после `upgrade head` и громко
предупреждает, если модели разошлись с базой (обычно это и значит
«поменяли модель, а ревизию не написали»).

Обратная сторона: модели обязаны описывать схему точно, включая
индексы с нестандартными именами и частичные уникальные индексы
(`part_categories`, `firmware_types`, `attachments`,
`platform_variant_*_requirements` — объявлены явно в `__table_args__`).
Иначе `--autogenerate` предложит удалить то, чего не видит в моделях, —
а это как раз индексы, которые держат уникальность имён категорий.

### Где физически хранятся данные

БД и загружаемые файлы (`variant_files`/`attachments`, см. «Файлы
исполнения» выше) теперь лежат в bind-mount директориях на хосте, не в
анонимных Docker volume'ах — `docker-compose.yml` использует
`${DATA_DIR:-./data}/postgres` и `${DATA_DIR:-./data}/uploads`
(настраивается через `.env`, см. `.env.example`). Это осознанный выбор
именно из-за прежней проблемы: анонимный volume ничего не бэкапит сам
по себе и требует `docker volume inspect`, чтобы вообще найти данные на
диске.

Резервное копирование закрыто `scripts/backup.sh` (дамп БД + архив
`uploads`, ротация до `BACKUP_RETENTION` копий каждого — см.
`README.md` → «Бэкапы»). Автозапуск при перезагрузке хоста — заготовка
в `scripts/systemd/` (юниты НЕ установлены и не включены нигде, только
лежат в репозитории — см. `scripts/systemd/README.md` для ручной
установки, когда пользователь решит, что пора).

Примечание: на этой машине (тестовое окружение, данные в нём не важны)
текущий инстанс ещё подключён к *старым* именованным volume'ам
(`server-tracker_pgdata`, `server-tracker_variant_uploads`) — переход
`docker-compose.yml` на bind mount сам по себе их не переносит.
Следующий `docker compose up`/`scripts/deploy.sh` здесь просто поднимет
пустую БД под новыми путями — и это нормально для этой машины. На
реальном проде перед первым запуском по новой схеме нужно либо
переносить данные из старых volume'ов, либо (что вероятнее) сразу
разворачивать начисто по `scripts/deploy.sh` без старых volume'ов —
там их просто не будет.

## JSON API для машинных потребителей (`/api/v1`)

`app/routers/api_v1.py` + `app/schemas/api.py` + `app/api_auth.py` —
интерфейс для скриптов и LLM-агента (заказчик: агент на LangChain).
Ключевые решения, которые нельзя нарушать:

- **Роутер остаётся тонким и вызывает те же сервисы, что и веб.** Ни
  одного правила, продублированного в API. Порядок этапов, блокировка
  состава, «прошивка только на custom-детали», `audit_log` — всё это
  работает потому, что API проходит через `app/services/`, а не мимо.
  Новый эндпоинт, который делает запрос к БД сам, — ошибка ревью.
- **Коды — контракт, подписи — удобство.** Каждое enum-поле отдаётся
  парой: `status` (стабильный backend-код) и `status_label` (русский
  текст из `app/i18n.py`). Потребитель ветвится по коду, а человеку
  показывает подпись. Старый `app/services/export.py` отдаёт только
  подписи — это и есть пример того, как делать нельзя.
- **Время — ISO-8601 со смещением**, не строки «дд.мм.гггг ЧЧ:ММ МСК».
  Форматирование для человека — дело UI (`app/timezone.py`).
- **Ошибки структурированные и с подсказкой.** Каждое сервисное
  исключение маппится в `_ERRORS` (`app/routers/api_v1.py`) на
  устойчивый `code` + `hint`, называющий вызов, который исправит
  ситуацию. Новое исключение в сервисе — добавить строку в `_ERRORS` в
  том же изменении, иначе оно улетит 500-й.
- **`dry_run` у всех записей.** Реализован через
  `app/db.dry_run_session()`: сессия, привязанная к транзакции с
  `join_transaction_mode="create_savepoint"`, которая всегда
  откатывается. Так выполняется *настоящая* сервисная функция со всеми
  её проверками, а не отдельная «только валидация» — та неизбежно
  разошлась бы с боевым кодом.
- **Английские описания эндпоинтов.** Они попадают в OpenAPI, который
  читает модель, а не рендерится в браузере, — по языковой конвенции это
  сторона кода. Русский доходит до потребителя через `*_label`.
- **Аутентификация — один шов.** `require_api_role()` в
  `app/api_auth.py` — единственное место, где решается «кто это и что
  ему можно»; роутеры токена не видят вообще. Подробности — в
  «Токены API» ниже.

### Токены API (`app/models/api_token.py`, `app/services/api_tokens.py`)

Доступ к `/api/v1` — по bearer-токену из таблицы `api_tokens`, одна
строка на потребителя. Выпуск и отзыв — на `/api-tokens` (только
`admin`) или через `python -m app.create_api_token`. Пока не выпущено ни
одного токена, API закрыт и отвечает `api_disabled`.

Из строки токена берутся две разные вещи:

- **права** — собственная роль токена, но не выше роли пользователя, за
  которым он закреплён (`effective_role()` берёт минимум). Поэтому
  понижение или деактивация пользователя тут же ограничивает все его
  токены, и не нужно искать и править строки токенов;
- **личность** — настоящий `User`, чтобы `audit_log.user_id` был
  заполнен и «кто установил этот компонент» оставалось отвечаемым после
  того, как это сделал агент.

Хранится только SHA-256 от токена, plaintext не сохраняется и
показывается ровно один раз при создании. SHA-256, а не argon2 как у
паролей: токен — это 256 бит из `secrets.token_urlsafe`, словаря против
него нет, а argon2 стоил бы ~100 мс CPU на каждый запрос к API. Отзыв
мягкий (`revoked_at`), не `DELETE`, — по тому же принципу, что и
деактивация пользователей.

Роутеры получают `ApiPrincipal` (токен + пользователь + **эффективная**
роль). Ветвиться в них надо по `principal.role`, никогда по
`principal.user.role`: иначе токен на чтение, выданный инженеру, увидит
всё, что видит инженер.

### Что агент может через API

**ЭТУ ТАБЛИЦУ ОБНОВЛЯТЬ В ТОМ ЖЕ ИЗМЕНЕНИИ**, которое добавляет,
удаляет или меняет смысл эндпоинта — то же правило, что для
`app/i18n.py` и списка таблиц в «Схема данных». Устаревший каталог хуже
отсутствующего: агент строит инструменты по нему и по `/openapi.json`,
и расхождение между ними он не заметит, а просто начнёт ошибаться.

Чтение — роль `viewer` и выше:

| Эндпоинт | На какой вопрос отвечает |
|---|---|
| `GET /search?q=` | «Где сейчас эта деталь?» — по серийнику, артикулу, asset tag, MAC. Для детали резолвится изделие, в котором она стоит сейчас |
| `GET /items` | «Что сейчас в тестировании?» — список с фильтрами по исполнению и статусу, с пагинацией |
| `GET /items/{id}` | Полное состояние изделия: комплектность против эталона, установленные и снятые компоненты, прошивки, MAC, история этапов. Отсюда берутся id для всех записей |
| `GET /platforms` | Каталог платформ и исполнений — превратить название, сказанное человеком, в `variant_id` |
| `GET /variants/{id}` | Эталон исполнения: какие элементы, прошивки и MAC требуются. Источник валидных `slot_id` и `firmware_type_id` |
| `GET /part-units/{serial}` | История детали: везде, где она стояла, плюс её прошивки — вид для расследования отказов и РМА |
| `GET /part-categories` | Каталог видов деталей. Источник валидных `part_category_id` для элементов BOM |
| `GET /firmware-types` | Каталог типов прошивок. Источник валидных `firmware_type_id` для требований исполнения |

Запись по изделиям — роль `engineer` и выше, у каждой поддерживается
`dry_run`:

| Эндпоинт | Что делает |
|---|---|
| `POST /items` | Заводит изделие по исполнению. Стартует пустым |
| `PATCH /items/{id}` | Меняет заказчика, локацию, примечания. Пропущенное поле не трогается, явный `null` очищает. Asset tag и исполнение неизменяемы |
| `POST /items/{id}/events` | Отмечает этап жизненного цикла с `occurred_at = now()` |
| `POST /items/{id}/components` | Устанавливает деталь в элемент изделия, при необходимости заводя её в каталоге |
| `POST /items/{id}/components/{component_id}/remove` | Снимает компонент и возвращает деталь на склад. Не удаление: `platform_components` append-only, снятие дописывается в историю — поэтому POST, а не DELETE |
| `POST /items/{id}/firmware` | Дописывает версию прошивки в лог (не перезаписывает предыдущую) |
| `POST /items/{id}/mac` | Регистрирует MAC за изделием или за установленной в нём платой |

Запись по структуре — та же роль и тот же `dry_run`. Это эталон, по
которому потом проверяется вся серия, поэтому шаги отдельные: ошибка
видна на своём шаге, а не половиной применившейся пачки.

| Эндпоинт | Что делает |
|---|---|
| `POST /platforms` | Заводит платформу (семейство изделий). Своего BOM не имеет |
| `POST /platforms/{id}/variants` | Заводит исполнение под платформой, с пустым BOM |
| `POST /variants/{id}/slots` | Добавляет элемент BOM: имя позиции, вид детали, количество, обязательность |
| `POST /variants/{id}/firmware-requirements` | Объявляет, что у изделий этого исполнения должна быть записана такая прошивка |
| `POST /variants/{id}/mac-requirements` | Объявляет ожидаемый MAC по метке (`BMC`, `LAN1`) |
| `POST /part-categories` | Добавляет вид детали в каталог. Группа `custom`/`purchased` решает, можно ли писать прошивки |
| `POST /firmware-types` | Добавляет тип прошивки в каталог |

Удаление — единственные вызовы, которые разрушают, а не записывают.
`dry_run` у них передаётся **query-параметром**, а не в теле: часть
HTTP-стеков выбрасывает тело у `DELETE`, и молча проигнорированный
`dry_run` на разрушающем вызове — худший из возможных отказов этой
схемы. Вложения на диске при `dry_run` не трогаются (`unlink_files=False`
в сервисах): файловая система не входит в транзакцию, которая
откатывается, и unlink пережил бы откат.

| Эндпоинт | Роль | Когда откажет |
|---|---|---|
| `DELETE /items/{id}` | `engineer` | Изделие отгружено (`item_shipped`) |
| `DELETE /variants/{id}` | `admin` | Есть изделия этого исполнения, либо его собственная категория/тип прошивки используются где-то ещё |
| `DELETE /platforms/{id}` | `admin` | Под платформой остались исполнения |
| `DELETE /part-categories/{id}` | `engineer` | На категорию ссылается элемент BOM или заведённая деталь |
| `DELETE /firmware-types/{id}` | `engineer` | Тип требуется исполнением или по нему записаны версии |

Роли здесь совпадают с веб-интерфейсом намеренно: одно и то же действие
требует одной и той же роли, каким бы входом ни пользовались.

**Чего через API сознательно нельзя** — и это тоже часть контракта, а не
недоделка. Агент не управляет пользователями и токенами, не загружает и
не удаляет файлы, не меняет asset tag и исполнение уже заведённого
изделия, не удаляет отгруженные изделия и не проставляет даты этапов
задним числом. Расширять этот список только по явной просьбе
пользователя, а не потому, что «агенту было бы удобнее».

Отдельно про размер каталога: он вырос с десяти операций до двадцати
семи, и это против исходного принципа «мало хорошо названных
инструментов, между которыми модель уверенно выбирает». Новый эндпоинт
здесь — это цена, а не бесплатное расширение; при прочих равных лучше
расширить существующий вызов, чем завести соседний.

## Конвенции кода

- Бизнес-логика — в app/services/, роутеры — тонкие (валидация входа
  через Pydantic, вызов сервиса, сериализация ответа).
- Любая мутация данных — внутри одной транзакции SQLAlchemy.
- Не добавлять зависимости без необходимости — проект должен оставаться
  разворачиваемым одним `docker compose up` без интернета в момент
  запуска (только на этапе сборки образа).
- Миграции только через Alembic, руками схему в проде не трогать.

## Что дальше (roadmap для агента, по приоритету)

1. ~~Реализовать auth~~ — готово: регистрация первого admin через
   CLI-команду, логин/логаут, session middleware, RBAC-декоратор,
   управление пользователями (`/users`: список/создание/смена
   роли/деактивация/сброс пароля, см. «Роли и доступ» выше).
2. **CRUD для part_types и part_units — пока не сделано, но теперь блокирует
   реальное использование** (см. пункт 4): установка компонента на
   платформу ищет `part_unit` по серийнику, а завести `part_unit` сейчас
   негде. Сюда же — интерактивная запись версий прошивок
   (firmware_records) на карточке part_unit: форма "тип прошивки + слот
   образа (primary/backup) + версия", создающая новую строку лога, а не
   редактирующая одну; на карточке показывать текущую версию каждого
   (firmware_type, image_slot) через DISTINCT ON.
3. ~~CRUD для иерархии Платформа → Исполнение (конструктор)~~ — готово:
   `/platforms` (список/создание платформы), `/platforms/{id}` (карточка
   платформы со списком исполнений), `/platforms/{id}/variants/new`
   (создание исполнения), `/variants/{id}` (конструктор: список
   элементов + форма добавления элемента + требования по прошивкам +
   требования по MAC + список изделий этого исполнения). Доступ —
   `admin` и `engineer` (решение принято пользователем; изначально было
   оставлено на усмотрение). Иерархия трёхуровневая (Платформа →
   Исполнение → Изделие), а не двухуровневая, как было в первой версии
   конструктора — см. «Иерархия «Платформа → Исполнение → Изделие»» выше.
4. CRUD для platform_items (изделий) + управление составом —
   **готово**: `/variants/{id}/items/new` (создание изделия
   внутри исполнения), `/items/{id}` (карточка: чек-лист комплектности
   по элементам исполнения, список установленных компонентов,
   установка/снятие компонента по серийному номеру), `/items/{id}/stages`
   (кнопки-этапы `assembled`/`test_started`/`test_passed`/
   `test_passed_with_remarks`/`test_failed`/`shipped` с проверкой
   порядка и синхронизацией `platform_items.status`), `/items/{id}/history`
   (лента этапов + снятые компоненты), `/items/{id}/firmware-mac`
   (чек-лист и запись версий прошивок + управление MAC-адресами по
   требованиям исполнения), редактирование customer/location/notes.
5. ~~Поиск по серийнику/asset tag/MAC~~ — готово: `/search` (
   `app/services/search.py`) возвращает HTML-партиал (`search_results.html`),
   не JSON — раньше не отрисовывался вообще (JSON в HTMX-таргет, плюс у
   `<input>` не было `name="q"`, так что параметр вообще не уходил).
   Ищет по `part_units.serial_number`, `platform_items.asset_tag`,
   `mac_addresses.mac_address` (все — `ILIKE`, минимум 2 символа).
   Для найденной детали/MAC — резолвится и показывается текущее
   изделие, где она установлена (join через активный
   `platform_components`), если установлена.
6. Импорт из CSV (part_units и platform_items bulk).
7. Экспорт в CSV/XLSX.
8. UI: список изделий с фильтрами (HTMX) на дашборде `/` — карточка
   изделия с чек-листом элементов конструктора и историей компонентов уже
   сделана в рамках пункта 4, фильтры на списке изделий — ещё нет.
